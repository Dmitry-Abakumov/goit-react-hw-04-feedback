import styled from '@emotion/styled';
import { color, space, layout, flexbox, typography, grid } from 'styled-system';

const Box = styled('div')(color, space, layout, flexbox, typography, grid);

export default Box;
